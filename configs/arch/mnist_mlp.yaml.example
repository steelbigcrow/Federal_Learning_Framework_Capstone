# MNIST MLP 模型架构配置示例文件
# 复制此文件为 mnist_mlp.yaml 并根据需要修改

dataset: "mnist"
model: "mlp"
input_size: 784
hidden_sizes: [200, 200]
num_classes: 10

optimizer:
  name: "adamw"
  lr: 1e-3
  weight_decay: 0.0

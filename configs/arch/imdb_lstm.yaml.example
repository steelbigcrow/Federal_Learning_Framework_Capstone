# IMDB LSTM 模型架构配置示例文件
# 复制此文件为 imdb_lstm.yaml 并根据需要修改

dataset: "imdb"
model: "lstm"
max_seq_len: 256
vocab_min_freq: 2
embedding_dim: 128
hidden_size: 256
num_layers: 2
bidirectional: false

optimizer:
  name: "adamw"
  lr: 1e-3
  weight_decay: 0.0
